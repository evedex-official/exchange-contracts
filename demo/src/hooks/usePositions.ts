import React from "react";
import { Address } from "viem";
import { useReadContract } from "wagmi";

const usePositions = (address: Address) => {
  const {} = useReadContract({});
  return;
};

export default usePositions;
